name: firecrawl
type: tool
enabled: false
description: >
  Firecrawl web scraper — converts any webpage into clean, LLM-ready markdown.
  Handles JavaScript-rendered pages, paywalls (where permitted), and complex layouts.

# Requirements:
#   pip install firecrawl-py
#   FIRECRAWL_API_KEY environment variable must be set

# To enable:
#   1. pip install firecrawl-py
#   2. Set FIRECRAWL_API_KEY in your .env
#   3. Change enabled: true
